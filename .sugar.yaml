compose-app: docker-compose
env-file: .env
defaults:
  group: {{ env.ENV }}
groups:
  prod:
    project-name: ftpserver
    compose-path:
      - docker-compose.yaml
    env-file: .env
    services:
      default: ftp
      available:
        - name: ftp

  dev:
    project-name: ftpserver
    compose-path:
      - docker-compose.yaml
      - docker-compose.dev.yaml
    env-file: .env
    services:
      default: ftp
      available:
        - name: ftp